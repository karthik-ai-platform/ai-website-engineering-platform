# Continuation Prompt

Continue the AI Website Engineering Platform in `C:\Users\HP\Desktop\ai-website-engineering-platform` on branch `codex/m08-isolated-runner`.

Read `AGENTS.md`, the authoritative SRS PDF, `docs/IMPLEMENTATION_PLAN.md`, `docs/IMPLEMENTATION_STATUS.md`, `docs/DECISIONS.md`, and `docs/SESSION_HANDOFF.md`, then inspect Git state. Preserve concurrent work.

## Current checkpoint

M01 and M02 are complete. M02 completion record `b7203a6` is pushed; draft PR #2 remains open and GitHub CI run 31456298365 passed, including ephemeral PostgreSQL migration. Do not repeat completed work.

M03 is complete. Completion record `917d1f3` is pushed; draft PR #3 is stacked on `codex/m02-projects-rbac`; final GitHub CI run 31459194718, job 93679135438, passed full validation and ephemeral PostgreSQL migration.

M04 is complete. Completion record `84b0156` is pushed; draft PR #4 is stacked on `codex/m03-provider-framework`; final GitHub CI run 31463150845, job 93690568730, passed full validation and ephemeral PostgreSQL migration. Provider evidence remains explicitly mock/contract.

M05 is complete. Completion record `ff7fd6f` is pushed; draft PR #5 is stacked on `codex/m04-github-onboarding`. Final GitHub CI run 31466267638, job 93699691293, passed full validation and ephemeral PostgreSQL migration.

M06 is complete. Implementation `333d0d0` and checkpoint `c7f5bc0` are pushed; draft PR #6 is stacked on the completed M05 branch. GitHub CI run 31473572456, job 93722088032, passed full validation and ephemeral PostgreSQL migration. The accepted scope includes strict ChangeRequest/RequirementSpec/review contracts; all eight modes; immutable original prompt and revisioned corrections; durable tenant-scoped/idempotent PostgreSQL persistence with migration `0004`; authenticated create/review APIs; accessible `/changes/new` intake and review; deterministic attachment scanning; and an AI-controller-only Requirement role with denial-before-output evidence. ADR-015 records the decision.

M07 is complete. Foundation `5a49f97`, governed API `94d34b7`, and analysis-gate implementation `0c18efd` are pushed; draft PR #7 is stacked on completed M06. Final GitHub CI run 31494692860, job 93789359220, passed full validation and ephemeral PostgreSQL migration. The completed milestone includes versioned execution plan/policy/approval and specialized-analysis contracts; deterministic Low/Medium/High/Blocked classification; tenant-scoped persistence/idempotency/audit; authenticated planning and approval APIs; high-risk pre-mutation pause; current multi-gate approvals; blocked no-relaxation behavior; and typed completed Architecture/UI/Security evidence bound to requirement, base commit, and policy digest. ADR-016 records the decisions.

M07 completion record `7089a1e` is pushed. GitHub CI run 31495684020, job 93792676230, passed full validation and ephemeral PostgreSQL migration. Branch `codex/m08-isolated-runner` starts from that exact checkpoint; M08 implementation has not started.

M08 is in progress. Foundation `f512045` is pushed and draft PR #8 is stacked on completed M07. GitHub CI run 31502183703, job 93814733180, passed full validation and ephemeral PostgreSQL migration. This slice replaces the raw M03 runner seam with versioned isolation-profile, immutable workspace, shell-free executable-plus-argv command, result/evidence, cancellation, and cleanup contracts; deterministic tenant/run/base-commit/profile/allowlist/resource/filesystem/artifact policy; canonical profile hashing; and an explicitly non-isolating conformance fixture that refuses production-isolation claims. ADR-017 records the decision.

M08 orchestration/persistence implementation `2d385f1`, worker-boundary correction `cb28fe6`, and checkpoint `9fc8b11` are pushed. The service reauthorizes current project-scoped `run:execute` authority, recomputes current approvals, requires current policy/repository/base-commit evidence, validates runner responses, and owns provision/execute/cancel/cleanup transitions. Migration `0007` and the worker-owned PostgreSQL adapter persist tenant-scoped/idempotent workspace, command, artifact-reference, cancellation, cleanup, and append-only audit evidence without argv, environment, raw stdout/stderr, or secret values. ADR-018 records the decision. Pull-request CI run 31929973797/job 95123194324 and push CI run 31929972885/job 95123191756 passed full validation and ephemeral PostgreSQL migration at `9fc8b11`.

Documentation checkpoint `b3e49e9` is pushed; pull-request CI run 31930359565 and push CI run 31930357204 passed full validation and ephemeral PostgreSQL migration.

M08 production-adapter foundation `757820c` is pushed. ADR-019 selects Vercel Sandbox behind `@platform/vercel-sandbox-runner` with `@vercel/sandbox` pinned to GA 3.0.0. The package validates a strict versioned approved-image manifest, requires an exact SHA-256 image/profile binding and hardening attestations, plans non-persistent zero-port/empty-environment sessions with supported vCPU/fixed-memory coupling and deny-all or HTTPS-only domain allowlists, and verifies provider-reported image/resources/timeout/persistence/status/expiry/network policy after creation. A mismatch is stopped and rejected. Pull-request CI run 31933994202/job 95133007721 and push CI run 31933992460/job 95133002939 passed full validation and ephemeral PostgreSQL migration. No live sandbox was created, and no provider isolation acceptance is claimed.

M08 hardened image/broker definition `835e6d7` is pushed. It adds a digest-pinned two-stage OCI definition, canonical image specification digest `5039c6256a2e531a6e68ffa09ac862ced57551bd638996c101e12e61f4d9053d`, fixed `node`/`npm` paths, no `sudo` executable, non-root UID/GID 10001 execution, strict broker schemas, digest-bound Git-bundle checkout at the exact detached clean commit, fixed profile ceilings, install-script denial, process-group termination for time/output/filesystem violations, and bounded-output digest/count evidence. The provider must explicitly start the fixed broker privileged; the broker then drops permanently before repository or command execution. Docker is unavailable and this checkout is not linked to a Vercel project, so no image build, VCR publication, live Sandbox call, or production-isolation acceptance is claimed. Vercel custom images/VCR are public beta and require organization approval.

Image/broker validation: formatting/lint passed; typecheck and build passed 11/11 packages; unit 14 files / 75 tests; contract 9 files / 51 tests including 12/12 focused image/planner/broker tests; integration 15 files / 53 tests with 1 file / 1 live PostgreSQL test skipped; serialized migration validation 6 files / 20 tests; browser/accessibility 4 tests; secret scan 194 files; dependency tree exit 0; approved-network high-threshold audit exit 0 with the unchanged four moderate `esbuild` advisories. The aggregate command exceeded the local shell ceiling during integration, which passed independently. An incorrectly forwarded parallel migration attempt reproduced five known setup-hook timeouts while 15 tests passed; the direct serialized rerun passed 20/20.

Documentation checkpoint `46b9016` is pushed. Pull-request CI run 31939133330/job 95145638982 and push CI run 31939130424/job 95145632003 passed full validation and ephemeral PostgreSQL migration at that exact head.

M08 broker transport `6e3d4e7` is pushed. It adds strict five-minute checkout-bundle metadata, pre-transfer SHA-256/size/freshness validation, mode-`0600` staging, explicit provider privileged broker invocation with empty environment and bounded timeout/abort propagation, semantic result/request-binding validation, and fail-closed sandbox stop on staging/provider/evidence failure. The public execute path accepts only workspace/profile/command input and re-runs deterministic policy, so callers cannot stage a raw broker request or expose Git credentials/tool argv on the provider command line. Pull-request CI run 31940914749/job 95149861687 and push CI run 31940912030/job 95149856213 passed full validation and ephemeral PostgreSQL migration. No live provider call or isolation claim was made.

M08 checkout-source/lifecycle composition `0f6ff8d` is pushed. It adds a versioned provider-neutral credential-free bundle contract/source port; a GitHub source that delegates approved short-lived installation access to an injected client, verifies exact repository/commit/five-minute freshness/size evidence, and computes SHA-256 locally; and a complete Vercel `RunnerProviderPort` composition for provision, execute, cancel, and destroy. Lifecycle behavior includes verified image/session/checkout evidence, tenant/run scope, sequential and concurrent replay protection, deterministic command-policy re-evaluation, abort propagation, and fail-closed stop behavior. The supplied session store is explicitly process-local and non-durable, expected artifacts are rejected until capture exists, and ADR-020 records the boundary. Pull-request CI run 31943688082/job 95156439123 and push CI run 31943685240/job 95156432826 passed full validation and ephemeral PostgreSQL migration. Draft PR #8 remains open and unmerged. No live GitHub bundle client or Vercel Sandbox was invoked.

M08 digest artifact capture `c0cb6d0` and documentation checkpoint `1f310cc` are pushed. Broker requests now carry only authorized expected paths and count/byte limits. The broker resolves regular files beneath the workspace, rejects symlink/escape/change/limit failures, and returns ordered SHA-256/size attestations. The provider independently retrieves each file through the Sandbox SDK, recomputes digest/size, writes it through tenant-scoped `ArtifactStorePort` with the authorized media type and retention class, and accepts only matching protected references. Evidence/storage mismatch stops and destroys the workspace. ADR-021 records the decision. Push run 31946335727/job 95162811906 and pull-request run 31946337567/job 95162816533 passed full validation and ephemeral PostgreSQL migration at `1f310cc`. No live provider or artifact store was invoked.

M08 active-session recovery `29842a3` and documentation checkpoint `0d1cbec` are pushed. Missing transient SDK handles are reattached through installed `@vercel/sandbox` 3.0.0 using the deterministic organization/project/run/idempotency-derived name. Stored workspace/profile/plan bindings are revalidated before provider access; recovered image/resources/network/tags/status and exact unexpired original expiry are verified afterward. Lookup failure is retryable, mismatch stops and destroys, and command state is rechecked after asynchronous recovery to prevent duplicate broker execution. ADR-022 records the decision. Push run 31948698327/job 95168689833 and pull-request run 31948700739/job 95168696186 passed full validation and ephemeral PostgreSQL migration at `0d1cbec`. The supplied store remains process-local/non-durable, so persistent worker storage and durable dispatch are the next implementation boundary. No live Sandbox was invoked.

M08 persistent-session/durable-dispatch implementation `281f08c` and documentation checkpoint `cf02411` are pushed. The worker now owns PostgreSQL persistence for schema-validated credential-free sandbox plans, workspace/profile identity evidence, and completed command replays while SDK handles and active promises remain transient. Every session lookup is tenant/project scoped. Durable dispatch stores only a validated protected artifact reference plus safe actor/correlation/request-time context, uses canonical-digest idempotency and conditional claims, retries only observed typed retryable failures with bounded deterministic delay, and marks expired running leases terminal without re-invoking the handler. Migration `0008` makes session identity/plan evidence forward-immutable and replay/attempt evidence append-only. The runtime pump is non-overlapping and drains an active poll on shutdown. ADR-023 records the decision. Push run 31952443565/job 95177893989 and pull-request run 31952446306/job 95177901169 passed full validation and ephemeral PostgreSQL migration at `cf02411`. Protected artifact retrieval and validated dispatch-handler/server composition remain next; no live provider call or production-isolation acceptance is claimed.

M08 protected runner-dispatch composition `0770d49` and documentation checkpoint `cb0a2b8` are pushed. The strict versioned artifact envelope permits only service-attributed prepare/execute/cancel/cleanup commands. The worker resolves it through tenant-scoped `ArtifactReaderPort`, requires dedicated metadata, bounded size, matching SHA-256, fatal UTF-8 JSON, exact inner/outer dispatch context, and current actor evidence, then delegates current authorization and state changes to `RunnerOrchestrationService`. Only an explicit typed retryable failure can reach bounded queue retry. The composition root wires the PostgreSQL queue/store, orchestration service, verified handler, and non-overlapping runtime pump. `WORKER_RUNNER_DISPATCH_ENABLED=true` fails startup unless protected artifact and isolated-runner providers are explicitly supplied; there is no local fallback. ADR-024 records the decision. Push run 31954943701/job 95184036367 and pull-request run 31954945300/job 95184040308 passed full validation and ephemeral PostgreSQL migration at `cb0a2b8`. No live provider or production-isolation acceptance is claimed.

M08 durability failure-injection and recovery-operations implementation `a77fabe` and documentation checkpoint `c72ece2` are pushed. A reconstructed PostgreSQL dispatch adapter claims persisted queued work, refuses a persisted retry wait before its durable timer, and processes it once due. The recovery runbook maps all dispatch states, requires tenant/project-scoped diagnostics plus provider/session/artifact reconciliation, and forbids manual ledger mutation or automatic replay of uncertain expired leases. ADR-007 remains deferred because the comparative production benchmark, durable human waits, cancellation/compensation, full operations evidence, and live crash/recovery remain open. Push run 31956425194/job 95187651199 and pull-request run 31956426895/job 95187655653 passed full validation and ephemeral PostgreSQL migration at `c72ece2`.

ADR-007 comparative benchmark revision 1 is committed at `3485446`. `docs/ADR_007_DURABILITY_BENCHMARK.md` maps the existing PostgreSQL ledger, Temporal, and Vercel Workflow against every mandatory gate and defines one common live failure-injection workload. The current ledger remains the narrow M08 privileged-dispatch component and is not eligible as the complete production engine in its present shape. Temporal and Vercel Workflow remain unselected finalists. Workflow SDK 5 durable in-flight cancellation remains beta while the observed stable package is 4.6.0. No dependency was installed and no provider/workflow run was invoked. ADR-007 remains deferred pending identical organization-approved live exercises and approved provider/data/region/retention/support/cost terms.

Broker-transport validation: formatting/lint passed; typecheck and build passed 11/11 packages; unit 14 files / 75 tests; contract 9 files / 51 tests; integration 16 files / 61 tests with 1 file / 1 live PostgreSQL test skipped; direct serialized migration validation 6 files / 20 tests; browser/accessibility 4 tests; secret scan 196 files; dependency tree exit 0; approved-network high-threshold audit exit 0 with the unchanged four moderate `esbuild` advisories. Focused transport/session verification passed 10/10 integration tests; `git diff --check` passed.

Checkout-source/lifecycle validation: formatting/lint passed; typecheck and build passed 11/11 packages; unit 14 files / 75 tests; contract 10 files / 56 tests; integration 17 files / 65 tests with 1 file / 1 live PostgreSQL test skipped; direct serialized migration validation 6 files / 20 tests; browser/accessibility 4 tests; secret scan 200 files; dependency tree exit 0; approved-network high-threshold audit exit 0 with the unchanged four moderate `esbuild` advisories. Focused source/lifecycle verification passed 9/9; `git diff --check` passed. The sandboxed audit request failed at the registry boundary; the approved-network rerun passed.

Adapter validation: formatting/lint passed; typecheck and build passed 11/11 packages; unit 14 files / 75 tests; contract 8 files / 44 tests; integration 15 files / 53 tests with 1 file / 1 live PostgreSQL test skipped; serialized migration validation 6 files / 20 tests; browser/accessibility 4 tests; secret scan 187 files; dependency tree exit 0; approved-network high-threshold audit exit 0 with the unchanged four moderate `esbuild` advisories. Focused adapter evidence is 5/5 planner contract and 2/2 verified-session integration tests. The aggregate and incorrectly forwarded migration reruns reproduced five known local PGlite setup-hook timeouts while 15 tests passed; the correctly serialized workspace command passed 20/20.

Local M08 orchestration evidence: formatting/lint passed; typecheck and build passed 10/10 packages; unit 14 files / 75 tests; contract 7 files / 39 tests; integration 14 files / 51 tests with 1 file / 1 live PostgreSQL test skipped; serialized migration validation 6 files / 20 tests; browser/accessibility 4 tests; secret scan 178 files; dependency tree exit 0. Approved-network `npm run security:deps` exited 0 with the unchanged four moderate `esbuild` advisories. One aggregate migration run had two known 10-second PGlite setup-hook timeouts while 18 tests passed; the no-file-parallelism rerun passed 20/20.

Implementation `94d34b7` and checkpoint `8ee4b4a` remain historical M07 service/API evidence. Do not repeat them.

Final M07 local evidence: formatting/lint passed; typecheck and build passed 10/10 packages; unit 13 files / 70 tests; contract 6 files / 36 tests; integration 11 files / 40 tests with 1 file / 1 live PostgreSQL test skipped; serialized migration validation 5 files / 17 tests; browser/accessibility 4 tests; secret scan 168 files. Approved-network `npm run security:deps` exited 0 with the unchanged four moderate `esbuild` advisories. Missing/mismatched/stale analysis fixtures stop before persistence, and incomplete controller evidence stops without retry. The parallel PGlite setup timeouts were infrastructure-only; the serialized rerun and GitHub ephemeral PostgreSQL step passed.

Local M06 evidence: formatting/lint passed; 10/10 typecheck and build; unit 12 files / 56 tests; contract 5 files / 32 tests; integration 9 files / 29 tests with 1 file / 1 live PostgreSQL test skipped; migration 4 files / 14 tests; browser/accessibility 4 tests; secret scan 158 files; dependency tree exit 0. Approved-network `npm run security:deps` exited 0 with the unchanged four moderate `esbuild` advisories. The sandboxed audit transport failure is infrastructure-only.

Local M05 evidence: formatting/lint passed; typecheck and build passed 10/10 packages; unit 11 files / 44 tests; contract 4 files / 27 tests; integration 7 files / 23 tests with 1 file / 1 live PostgreSQL test skipped; migration 3 files / 11 tests; browser/accessibility 3 tests; corrected secret scan 147 files. Approved-network `npm run security:deps` exited 0 with the known 4 moderate `esbuild` advisories; `npm ls --omit=dev --all` exited 0.

Host GitHub CLI authentication is valid for `karthik18mohan`; sandboxed processes cannot read the host keyring, so authenticated GitHub CLI operations require the approved host execution path. Never expose credentials or merge autonomously.

Artifact-capture validation: formatting/lint passed; typecheck and build passed 11/11 packages; unit 14 files / 75 tests; contract 10 files / 56 tests; integration 17 files / 67 tests with 1 file / 1 live PostgreSQL test skipped; migration validation 6 files / 20 tests; browser/accessibility 4 tests; secret scan 200 files; dependency tree and `git diff --check` passed. Focused artifact/broker/contract verification passed 17/17. The aggregate run stopped only when the sandboxed audit could not reach the registry; the approved-network audit rerun exited 0 with the unchanged four moderate `esbuild` advisories.

Safe-recovery validation: formatting/lint passed; typecheck and build passed 11/11 packages; unit 14 files / 75 tests; contract 10 files / 56 tests; integration 17 files / 70 tests with 1 file / 1 live PostgreSQL test skipped; direct single-worker migration validation 6 files / 20 tests; browser/accessibility 4 tests; secret scan 200 files; dependency tree and `git diff --check` passed. Focused recovery/session/broker verification passed 3 files / 19 tests. Two aggregate migration attempts had setup-hook timeouts in different untouched files; the single-worker rerun passed 20/20. The sandboxed registry audit failed at the network boundary; the approved-network high-threshold rerun exited 0 with the unchanged four moderate `esbuild` advisories.

Durable-dispatch validation: formatting/lint passed; typecheck and build passed 11/11 packages; unit 14 files / 76 tests; contract 10 files / 56 tests; integration 18 files plus 1 skipped live PostgreSQL file with 73 tests passed and 1 skipped; direct single-worker migration validation 6 files / 20 tests; browser/accessibility 4 tests; secret scan 204 files; dependency tree and `git diff --check` passed. Focused persistence/dispatch/provider/runtime/migration verification passed 5 files / 25 tests. The aggregate migration phase had one known resource-sensitive 10-second setup-hook timeout in an untouched M04 test while 19/20 passed; the direct single-worker rerun passed 20/20. The sandboxed registry audit failed at the network boundary; the approved-network high-threshold rerun exited 0 with the unchanged four moderate `esbuild` advisories.

Protected-dispatch validation: formatting/lint passed; typecheck and build passed 11/11 packages; unit 15 files / 82 tests; contract 11 files / 58 tests; integration 18 files plus 1 skipped live PostgreSQL file with 74 tests passed and 1 skipped; migration 6 files / 20 tests; browser/accessibility 4 tests; secret scan 209 files; dependency tree and `git diff --check` passed. Focused handler/config tests passed 9/9, dispatch contracts 2/2, and durable runtime composition 4/4. The first full attempt stopped only on formatting; corrected clean reruns reached only the sandboxed audit network boundary, and approved-network audit exited 0 with the unchanged four moderate `esbuild` advisories.

Durability-evidence validation: focused integration passed 1 file / 5 tests; formatting/lint passed; typecheck and build passed 11/11 packages; unit 15 files / 82 tests; contract 11 files / 58 tests; integration 18 files plus 1 skipped live PostgreSQL file with 75 tests passed and 1 skipped; direct single-worker migration validation passed 6 files / 20 tests; browser/accessibility 4 tests; secret scan 210 files; dependency tree and `git diff --check` exited 0. The aggregate migration phase reproduced three known PGlite setup-hook timeouts while 17/20 passed; the serialized rerun passed 20/20. The sandboxed audit failed at the registry boundary; the approved-network high-threshold audit exited 0 with the unchanged four moderate `esbuild` advisories.

ADR-007 benchmark-record validation: formatting/lint passed; typecheck and build passed 11/11 packages; unit 15 files / 82 tests; contract 11 files / 58 tests; integration 18 files plus 1 skipped live PostgreSQL file with 75 tests passed and 1 skipped; direct single-worker migration validation passed 6 files / 20 tests; browser/accessibility 4 tests; secret scan 211 files; dependency tree and `git diff --check` exited 0. The aggregate migration phase reproduced two known PGlite setup-hook timeouts while 18/20 passed; the serialized rerun passed 20/20. The sandboxed audit failed at the registry boundary; the approved-network high-threshold audit exited 0 with the unchanged four moderate `esbuild` advisories.

## Next exact tasks

1. In an organization-approved non-production Vercel project, build the image from `835e6d7`, publish it through VCR, resolve its immutable digest, and run the documented self-checks. Do not claim success until observed.
2. Supply and validate the concrete organization-approved GitHub installation bundle client behind the new short-lived-access seam; never log, persist, or stage the access material.
3. Obtain organization-approved non-production Temporal Cloud and Vercel Workflow benchmark scopes, run the identical fixture in `docs/ADR_007_DURABILITY_BENCHMARK.md`, approve provider/data/region/retention/support/cost terms, and accept the follow-up production-engine ADR before calling M08 or M19 production-complete.
4. Select and compose an organization-approved protected artifact read/write adapter plus the approved runner/GitHub dependencies into `createConfiguredWorkerRuntime`; enable dispatch only after the server composition has no local or memory fallback.
5. Then obtain live forbidden host filesystem/process/network/credential/production-secret, resource-limit, malicious-install-script, cancellation, cleanup, recovery, crash/restart, and artifact-integrity evidence. Never cite the conformance fixture or local composition tests as production isolation. Do not make a live model call, deploy production, or merge autonomously.

Never expose secrets, invoke an LLM outside the AI Cost Controller, weaken tenant scoping or append-only audit, production-deploy, modify production DNS/domains/secrets, force-push, push to `main`, merge a PR, or reset a non-local database.
